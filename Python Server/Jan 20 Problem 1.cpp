static void computeAllScores(const Set<GPoint>& landMines, Map<GPoint, int>& scores) {
	for (GPoint p : landMines) {
		// calculate the score for the mine 
		Queue<GPoint> detonate; 
		int time = 0; 
		while (!detonate.isEmpty()) {
			
		}
	}
}